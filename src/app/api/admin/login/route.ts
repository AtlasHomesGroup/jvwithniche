import { notImplemented } from "@/lib/api";

export const runtime = "nodejs";

// Admin password login (Milestone 6). Returns an httpOnly session cookie.
export async function POST() {
  return notImplemented("M6 — admin login");
}
