import { notImplemented } from "@/lib/api";

export const runtime = "nodejs";

// Autosave partial form state against a draft session token (Milestone 2).
export async function PATCH() {
  return notImplemented("M2 — draft autosave endpoint");
}
